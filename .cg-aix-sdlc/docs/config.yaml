# Phase 0 (Eval) Configuration
# Generated: 2026-01-21T11:13:00Z

project:
  name: langbuilder
  path: E:/Work/CloudGeometry/langbuilder
  language: Python/TypeScript
  framework: FastAPI + React

repository:
  size_category: Large
  source_files: 2524
  lines_of_code: 150000  # Estimated based on file counts

monorepo:
  is_monorepo: true
  tool: uv-workspace
  package_count: 2
  packages:
    - name: langbuilder
      path: langbuilder
      type: main
    - name: langbuilder-base
      path: langbuilder/src/backend/base
      type: library

orchestration:
  mode: "standard"           # Options: quick, standard, detailed
  parallel: false            # Enable safe parallel execution
  max_concurrent: 3          # Max parallel chunks (2-4)

scope:
  architecture: true
  product: true
  ai_context: true
  testing: true
  onboarding: true
  landscape_research: false  # Optional web-based research

validation:
  enabled: true
  auto_fix_enabled: true     # Auto-fix loop for extraction validation
  quality_threshold: 80      # Minimum quality score (%)

extraction:
  profile_extraction: true   # Use intermediate JSON profiles
  chunk_budget:
    max_read: 10             # Default max files per chunk
    max_grep: 5              # Default max grep searches per chunk
